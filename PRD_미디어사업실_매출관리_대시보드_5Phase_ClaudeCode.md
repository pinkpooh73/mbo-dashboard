# PRD: 미디어사업실 매출관리 대시보드 고도화 (Claude Code 인수인계용 · 5-Phase)

| | |
|---|---|
| **작성일** | 2026-07-31 |
| **작성자** | Robin (미디어사업실) / Claude 정리 |
| **문서 상태** | Ready for Development |
| **개발 주체** | Claude Code |
| **운영 담당** | Robin (직접 개발/운영) |

---

## 0. 한눈에 보기

기존에는 Claude와의 대화를 통해 매번 수동으로 HTML 대시보드를 재생성해왔음. 이를 **① 데이터/화면 분리 → ② 자동 동기화 → ③ 자동 배포(로그인 포함) → ④ 실시간성 강화 → ⑤ 안정화**의 5단계로 재개발하여, 사람 개입 없이 항상 최신 상태로 유지되는 웹 대시보드로 전환하는 것이 목표.

| Phase | 한 줄 요약 | 선행조건 |
|---|---|---|
| **1. 데이터/화면 분리** | JS 하드코딩 데이터를 JSON으로 분리 + 과거 스냅샷 이관 | 없음 |
| **2. Sync Job** | Google Sheet → JSON 자동 동기화 (1시간 폴링) | Phase 1 |
| **3. 배포 & 접근제어** | 로그인 페이지 + GitHub Pages 자동 배포 | Phase 1, 2 |
| **4. 실시간성 고도화** | 폴링 → Apps Script 트리거 전환, 스냅샷 자동 적재 | Phase 2 안정화 후 |
| **5. 안정화** | 이상치 알림, 신규 상품 자동 대응, 규칙 문서화 | Phase 1~4 |

Phase 6(사내 호스팅 서버 이관)은 **현재 범위 밖**이며 7장에 향후 계획으로만 기록.

---

## 1. 배경 및 문제 정의

### 1.1 현재 방식
'26년 미디어사업실 KPI 달성현황 대시보드는 단일 HTML 파일(`dashboard_YYYYMMDD.html`)로 운영 중이며, 다음과 같은 수동 프로세스로 갱신됨.

1. Google Sheets("2026년_미디어사업실 매출 관리_v2.0")를 Claude가 매번 수동 요청 시 다운로드
2. 시트 값을 읽어 KPI/상품/팀 데이터를 계산 후, HTML 내부에 **하드코딩된 JS 상수**(`PRODS`, `TOTAL`, `TKPIB` 등)로 치환
3. "KPI 데이터" 원본 뷰 탭은 별도의 **정적 HTML 문자열**로 재생성 필요 (누락되기 쉬움)
4. "주차별 핵심 지표 비교"는 Claude가 대화 메모리에 캐싱해 둔 지난 스냅샷과 비교해 수동 계산
5. 완성된 파일을 사용자에게 다운로드 형태로 전달 → 자동 호스팅 없음

### 1.2 문제점
| 구분 | 문제 |
|---|---|
| 실시간성 | 시트가 바뀌어도 Claude에게 요청하기 전까지 반영되지 않음 |
| 신뢰성 | 데이터/렌더링이 한 파일에 얽혀 있어 갱신 시 특정 탭이 누락된 사례 발생 |
| 확장성 | 신규 상품 추가, 시트 구조 변경 시마다 다수 지점을 수작업으로 동기화 |
| 공유성 | 매번 파일을 다운로드해서 전달 → 팀 내 상시 접근 가능한 URL 없음 |
| 운영 비용 | 매 갱신마다 대화 토큰과 Claude 세션 필요 (자동화 불가) |

---

## 2. 목표 및 Non-goals

**목표**
1. 실시간 매출 데이터 갱신 — Google Sheet 변경분이 사람 개입 없이 자동 반영
2. 웹 버전 자동 생성/배포 — 고정 URL로 상시 접근 가능
3. Claude Code 기반 재개발 — 유지보수 가능한 코드베이스 전환

**Non-goals (이번 범위 제외)**
- 다른 사업부/시트로의 확장
- 모바일 네이티브 앱 개발
- Google Sheet 자체의 입력 UI 개선

---

## 3. 확정된 의사결정 (착수 전 필독)

| 항목 | 결정 |
|---|---|
| 접근 권한 | 로그인 페이지 + 고정 비밀번호(`aleldjtkdjq1!`) 방식 (Phase 3에서 구현, 보안 한계는 8장 리스크 참고) |
| 개발/운영 담당 | Robin이 직접 개발·운영 |
| 과거 스냅샷 이관 | Claude 메모리에 있던 스냅샷(7/16, 7/23, 7/24, 7/30, 7/31)을 `snapshotHistory` 초기값으로 이관 (Phase 1) |
| 호스팅 환경 | 1차: GitHub Pages + GitHub Actions. **추후 사내 호스팅 서버로 이관 예정** (7장 참고) |
| Google Sheets API 접근 권한 | 확보됨 (Robin 계정/서비스 계정 권한 보유) |
| 실시간 갱신 주기 | 1차 1시간 폴링 → Phase 4에서 Apps Script 트리거로 준실시간 전환 |
| 취급고 총계 제외 규칙 | 용역사업·다윈의 취급고는 매출과 중복이므로 전체 취급고 총계·팀 합계에서 항상 제외 (개별 상품 표에는 정상 표시) |

---

## 4. 아키텍처 개요

### 4.1 핵심 설계 원칙: 데이터와 화면의 분리
```
[Google Sheet] → [Sync Job] → [data.json + snapshotHistory] → [Web Frontend]
                                                                      ↑
                                                        [GitHub Actions → GitHub Pages]
```

### 4.2 컴포넌트

| 컴포넌트 | 역할 | 기술 |
|---|---|---|
| Sync Job | 시트 파싱 → 정규화된 JSON 생성 | Google Sheets API, GitHub Actions cron (1시간) → 추후 Apps Script 트리거 |
| Data Store | 최신 데이터 + 과거 스냅샷 | JSON 파일 (Git 커밋 관리) |
| Frontend | 기존 UI/UX 100% 유지 (탭, 차트, 파스텔 디자인) | 기존 Vanilla JS 구조 유지 (필요 시 React/Vite 검토) |
| 접근 제어 | 로그인 게이트 | 클라이언트 비밀번호 체크 (1차) |
| CI/CD | 자동 빌드/배포 | GitHub Actions + GitHub Pages |

---

## 5. 데이터 모델

```json
{
  "updatedAt": "2026-07-31T09:00:00+09:00",
  "sourceSheetVersion": "v2.0",
  "totals": {
    "kpi_b": [...12개월...], "kpi_r": [...],
    "act_b": [...], "act_r": [...]
  },
  "products": {
    "비즈링": {
      "team": "biz",
      "kpi_b": [...], "kpi_r": [...],
      "act_b": [...], "act_r": [...],
      "excludeFromBillTotal": false
    },
    "용역사업": { "...": "...", "excludeFromBillTotal": true },
    "다윈": { "...": "...", "excludeFromBillTotal": true }
  },
  "snapshotHistory": [
    { "date": "2026-07-16", "totals": {...}, "products": {...} },
    { "date": "2026-07-23", "totals": {...}, "products": {...} },
    { "date": "2026-07-24", "totals": {...}, "products": {...} },
    { "date": "2026-07-30", "totals": {...}, "products": {...} },
    { "date": "2026-07-31", "totals": {...}, "products": {...} }
  ]
}
```

`excludeFromBillTotal` 플래그로 "인식x" 같은 계산 규칙을 코드가 아닌 **데이터로 관리**. `snapshotHistory`가 주차별 비교를 위한 Claude 메모리 캐싱을 완전히 대체.

---

## 6. Phase별 실행 계획

### Phase 1 — 데이터/화면 분리 + 과거 이력 이관
**목표**: 하드코딩된 JS 데이터와 렌더링 로직을 분리해 유지보수 가능한 구조로 전환

**작업 범위**
- 기존 HTML에 박혀 있던 `PRODS`/`TOTAL`/`TKPIB`/`TACTB` 등 JS 상수를 5장 스키마의 `data.json`으로 추출
- 프론트엔드가 `fetch('data.json')`으로 데이터를 읽어 렌더링하도록 변경 (탭 구조·차트·파스텔 디자인은 100% 동일하게 유지)
- 용역사업·다윈에 `excludeFromBillTotal: true` 반영, 총계 계산 로직이 이 플래그를 참조하도록 구현
- Claude 메모리의 과거 스냅샷 5개(7/16, 7/23, 7/24, 7/30, 7/31)를 `snapshotHistory` 초기값으로 이관

**완료 기준 (Acceptance Criteria)**
- [ ] 6개 탭(KPI달성현황/미디어사업팀/미디어마케팅팀/대행사별매출/월별캠페인/KPI데이터) 모두 기존과 동일하게 렌더링
- [ ] `data.json` 값만 수정하면 코드 변경 없이 화면이 갱신됨을 로컬에서 검증
- [ ] `snapshotHistory`에 5개 스냅샷이 모두 포함되고, 주차별 비교 탭이 정상 동작
- [ ] 용역사업·다윈 취급고가 개별 상품 표에는 표시되지만 전체/팀 총계에서는 제외됨을 확인

---

### Phase 2 — Sync Job (자동 데이터 동기화)
**목표**: Google Sheet 변경분이 사람 개입 없이 `data.json`에 자동 반영

**작업 범위**
- Google Sheets API 연동 스크립트 작성 (Robin 보유 권한으로 서비스 계정/API 접근 구성)
- GitHub Actions cron으로 1시간 간격 폴링 스케줄러 구성
- 셀 라벨 검증 로직 추가 (예: "취급고(인식x)" 라벨을 확인해 총계 제외 여부를 자동 판단)
- 시트 구조가 예상과 다를 경우 조용히 넘어가지 않고 갱신 중단 + 에러 로그 기록

**완료 기준**
- [ ] 시트 값을 수정한 뒤 최대 1시간 이내 `data.json`에 반영됨을 실증
- [ ] 시트 라벨/구조 불일치 시 명시적 에러가 발생하고 잘못된 데이터로 덮어쓰지 않음
- [ ] 신규 상품 행 추가 시 최소한 알림이 발생하거나 자동 반영됨

---

### Phase 3 — 배포 & 접근 제어
**목표**: 팀이 상시 접근 가능한 고정 URL 제공, 최소한의 접근 제어 적용

**작업 범위**
- 로그인 페이지 구현: 비밀번호(`aleldjtkdjq1!`) 입력 후 대시보드 진입, 인증 상태를 로컬/세션 스토리지에 저장
- GitHub Actions 기반 CI/CD 구성: 코드 커밋 또는 `data.json` 갱신 시 GitHub Pages 자동 배포
- 헤더에 "마지막 업데이트: YYYY-MM-DD HH:mm" 표시

**완료 기준**
- [ ] 비밀번호 미입력 시 대시보드 콘텐츠에 접근 불가
- [ ] `data.json` 갱신 시 별도 명령 없이 GitHub Pages에 자동 반영
- [ ] 고정 URL로 실제 팀원이 접근 가능함을 확인
- [ ] "GitHub Pages URL은 사실상 공개"라는 리스크(8장)를 Robin이 인지하고 있음을 재확인

---

### Phase 4 — 실시간성 고도화
**목표**: 폴링 방식의 최대 1시간 지연을 개선하고 스냅샷 적재를 완전 자동화

**작업 범위**
- Google Apps Script `onEdit`/`onChange` 트리거 → Webhook으로 Sync Job 즉시 호출하는 방식으로 전환
- 매 동기화 시점의 데이터를 `snapshotHistory`에 자동 적재 (수동 캐싱 완전 제거)

**완료 기준**
- [ ] 시트 수정 후 1분 내외로 데이터가 반영됨을 확인
- [ ] 별도 개입 없이 주차별 비교가 최신 스냅샷 기준으로 자동 계산됨

*(선행조건: Phase 2가 최소 1~2주 안정 운영된 이후 착수 권장)*

---

### Phase 5 — 안정화
**목표**: 운영 신뢰성 확보 및 장기 유지보수성 강화

**작업 범위**
- 이상치 감지 로직 구현 (예: 취급고=매출 완전 동일, 전월 대비 비정상 급증 등) + Slack/이메일 알림
- 신규 상품 자동 대응 로직 고도화 (팀 분류, KPI 존재 여부 등 메타데이터 관리 체계화)
- 계산 규칙(용역사업/다윈 제외 등)을 설정 파일 또는 코드 주석으로 명문화해 후임자도 파악 가능하게 정리

**완료 기준**
- [ ] 의도적으로 이상치 데이터를 넣었을 때 알림이 정상 발송됨을 테스트로 검증
- [ ] 신규 상품을 시트에 추가했을 때 코드 수정 없이(또는 최소 수정으로) 대시보드에 반영됨
- [ ] 계산 규칙이 문서/코드에 명시되어 Robin 외 다른 사람도 유지보수 가능한 상태

---

## 7. 향후 계획 (범위 밖)

| 항목 | 내용 |
|---|---|
| **Phase 6 — 사내 호스팅 서버 이관** | GitHub Pages의 "URL 공개" 구조적 한계를 해소하기 위해, 추후 사내 호스팅 서버로 이관 예정. 이관 시 로그인 방식도 서버단 인증으로 전환 검토 |

---

## 8. 리스크 및 대응

| 리스크 | 영향 | 대응 방안 |
|---|---|---|
| Google Sheets API 인증/쿼터 제한 | 동기화 실패 | 서비스 계정 사용, 폴링 주기 최적화, 실패 시 재시도·알림 |
| 시트 행/열 구조가 예고 없이 바뀜 (실제 발생 이력: "취급고(인식x)" 라벨 변경) | 잘못된 데이터 반영 | 파싱 시 셀 라벨 검증 단계 추가, 불일치 시 갱신 중단 + 알림 (Phase 2) |
| 신규 상품 추가 시 팀 분류·KPI 존재 여부 등 예외 케이스 | 대시보드 누락/오류 | 상품 메타데이터를 시트 또는 설정 파일에서 명시적으로 관리 (Phase 5) |
| GitHub Pages는 Private 리포지토리여도 배포 URL 자체는 공개 접근 가능 | 매출 등 민감 정보 노출 리스크 | 1차는 구조적 한계로 인지하고 사용, Phase 6(사내 서버 이관)에서 근본 해결 |
| 클라이언트 하드코딩 로그인 비밀번호 | 소스보기로 그대로 노출되어 사실상 "누구나 열람 가능"과 동일 | Phase 6 이관 전까지의 **임시 조치**로 인지, 이관 시 서버단 인증으로 전환 |

---

## 9. 성공 지표

- 대시보드 갱신을 위한 **수동 개입(Claude 대화) 횟수 0회/주** (완전 자동화)
- 시트 변경 → 웹 반영까지 **평균 지연 시간 1시간 이내** (Phase 4 이후 1분 이내)
- 갱신 누락(예: KPI 데이터 탭 미반영) 사고 **0건**
- 팀 내 대시보드 URL 접근 빈도로 활용도 측정
