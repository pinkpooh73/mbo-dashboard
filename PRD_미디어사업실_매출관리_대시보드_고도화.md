# PRD: 미디어사업실 매출관리 대시보드 고도화

| | |
|---|---|
| **작성일** | 2026-07-31 |
| **작성자** | Robin (미디어사업실) / Claude 정리 |
| **문서 상태** | Draft v1.0 |
| **개발 주체** | Claude Code |
| **운영 담당** | Robin (직접 개발/운영) |

---

## 1. 배경 및 문제 정의

### 1.1 현재 방식
'26년 미디어사업실 KPI 달성현황 대시보드는 단일 HTML 파일(`dashboard_YYYYMMDD.html`)로 운영 중이며, 다음과 같은 수동 프로세스로 갱신되고 있음.

1. Google Sheets("2026년_미디어사업실 매출 관리_v2.0")를 Claude가 매번 수동 요청 시 다운로드
2. 시트 값을 읽어 KPI/상품/팀 데이터를 계산 후, HTML 내부에 **하드코딩된 JS 상수**(`PRODS`, `TOTAL`, `TKPIB` 등)로 치환
3. "KPI 데이터" 원본 뷰 탭은 별도의 **정적 HTML 문자열**로 재생성 필요 (누락되기 쉬움)
4. "주차별 핵심 지표 비교"는 Claude가 대화 메모리에 캐싱해 둔 지난 스냅샷과 비교해 수동 계산
5. 완성된 파일을 사용자에게 다운로드 형태로 전달 → **사용자가 직접 열람/공유**해야 함 (자동 호스팅 없음)

### 1.2 문제점
| 구분 | 문제 |
|---|---|
| 실시간성 | 시트가 바뀌어도 Claude에게 "갱신해줘"라고 요청하기 전까지 반영되지 않음 |
| 신뢰성 | 데이터/렌더링이 한 파일에 얽혀 있어 갱신 시 특정 탭(raw 데이터 등)이 누락된 사례 발생 |
| 확장성 | 신규 상품 추가, 시트 구조 변경(예: "인식x" 라벨링) 시마다 다수 지점을 수작업으로 동기화 |
| 공유성 | 매번 파일을 다운로드해서 전달 → 팀 내 상시 접근 가능한 URL 없음 |
| 운영 비용 | 매 갱신마다 대화 토큰과 Claude 세션 필요 (자동화 불가) |

---

## 2. 목표

1. **실시간 매출 데이터 갱신**: Google Sheet 변경분이 사람의 개입 없이 자동으로 대시보드에 반영
2. **웹 버전 자동 생성/배포**: 코드 또는 데이터가 갱신되면 자동으로 빌드·배포되어 고정 URL로 상시 접근 가능
3. **Claude Code 기반 재개발**: 현재의 "AI가 매번 HTML을 재작성"하는 구조에서 벗어나, **유지보수 가능한 코드베이스**로 전환

### 2.1 Non-goals (이번 범위 제외)
- 다른 사업부/시트로의 확장 (일단 미디어사업실 전용)
- 모바일 네이티브 앱 개발
- 시트 자체의 데이터 입력 UI 개선 (Google Sheet는 입력 소스로 계속 사용)

---

## 3. 제안 아키텍처

### 3.1 핵심 설계 원칙: **데이터와 화면의 분리**
현재 가장 큰 구조적 문제는 데이터(숫자)와 프레젠테이션(HTML/JS)이 한 파일에 뒤섞여 있다는 점. 이를 분리하는 것이 고도화의 전제 조건.

```
[Google Sheet] → [Sync Job] → [Data Store(JSON/DB)] → [API] → [Web Frontend]
                                                              ↑
                                                    [Static Build/Deploy]
```

### 3.2 컴포넌트

| 컴포넌트 | 역할 | 후보 기술 |
|---|---|---|
| **Sync Job** | 시트 변경 감지 → 파싱 → 정규화된 JSON 생성 | Google Apps Script(onEdit 트리거) 또는 주기적 폴링(Cloud Function/GitHub Actions cron) |
| **Data Store** | 정규화된 최신 데이터 + 과거 스냅샷(주차별 비교용) 보관 | 우선 JSON 파일(Git 커밋) → 필요 시 경량 DB(Supabase, SQLite) |
| **Backend/API** | 프론트에 데이터 제공, 스냅샷 비교 로직 수행 | Node.js 경량 API 또는 정적 JSON 서빙으로 시작 |
| **Frontend** | 현재 대시보드 UI/UX 재구현 (탭, 차트, 파스텔 디자인 시스템 유지) | 기존 Vanilla JS 유지 또는 React/Vite로 마이그레이션 |
| **CI/CD** | 코드/데이터 변경 시 자동 빌드 및 배포 | GitHub Actions + **GitHub Pages** (확정) |

### 3.3 실시간 갱신 방식 (택 1, 우선순위 순)
1. **주기적 폴링(권장, 우선 적용)**: **1시간 간격**으로 Google Sheets API 조회 → 변경분 diff 계산 후 갱신. 구현이 간단하고 API 호출량 부담이 적어 초기 단계에 적합
2. **Google Apps Script 트리거 방식**: 시트에 `onEdit`/`onChange` 트리거를 걸어 변경 시 즉시 Webhook 호출 → 지연 없이 갱신. 폴링보다 구현 복잡도가 높아 안정화 이후 전환 검토
3. **수동 리프레시 버튼**: 최소 구현체로, 웹에서 버튼 클릭 시 API 호출해 최신화 (1안 구현 전 임시 단계로 활용 가능)

> **권장**: 1단계는 **1시간 폴링**으로 구현 → 운영 안정화 후 필요 시 Apps Script 트리거(준실시간)로 전환

---

## 4. 기능 요구사항

### 4.1 기존 기능 (반드시 유지)
- [ ] KPI 달성현황 탭: 전체/분기별 취급고·매출 달성률 카드 및 진행률 바
- [ ] 미디어사업팀 / 미디어마케팅팀 탭: 팀별·상품별 실적 차트 및 표
- [ ] 대행사별 매출 탭
- [ ] 월별 캠페인 탭
- [ ] KPI 데이터 탭 (원본 시트 뷰, 조건부 서식 포함)
- [ ] 주차별 핵심 지표 비교 / 상품별 주요 변동
- [ ] 파스텔 톤 디자인 시스템 (헤더 색상, 카드 그리드, 타이포 스케일)

### 4.2 신규 기능
- [ ] **실시간 동기화**: 시트 변경 → **1시간 이내** 웹 대시보드 자동 반영
- [ ] **자동 웹 배포**: 데이터/코드 갱신 시 별도 명령 없이 자동 빌드·배포, 고정 URL 유지
- [ ] **갱신 이력 자동 스냅샷**: 매 동기화 시점 데이터를 자동 저장 → "주차별 비교"를 수동 캐싱 없이 자동 계산
- [ ] **최근 갱신 시각 표시**: 헤더에 "마지막 업데이트: YYYY-MM-DD HH:mm" 노출
- [ ] **접근 제어(로그인)**: 비밀번호 입력 화면을 통과해야 대시보드 열람 가능 (4.4 참고)
- [ ] **신규 상품 자동 인식**: 시트에 새 상품 행이 추가되면 코드 수정 없이 대시보드에 자동 반영 (또는 최소한 관리자 알림)
- [ ] **데이터 검증 알림**: 이상치 감지 시(예: 취급고=매출 완전 동일, 전월 대비 비정상적 급증) Slack/이메일 알림

### 4.3 운영/관리 기능
- [ ] 특정 계산 규칙(예: 용역사업·다윈 취급고 총계 제외)을 **코드 레벨 설정값**으로 명시 (현재는 Claude 메모리에만 기록되어 있어 사람이 못 봄 → 코드화 필요)
- [ ] 시트 구조 변경 시 파싱 실패를 조기 감지하는 스키마 검증 로직

---

### 4.4 접근 제어 (로그인 페이지)

- 대시보드 진입 전 단일 비밀번호 입력 화면을 거치도록 구성
- 초기 비밀번호(안): `aleldjtkdjq1!`
- 인증 성공 시 세션/로컬 스토리지에 인증 상태를 저장해, 브라우저 재방문 시 재입력 없이 접근 가능하도록 함 (세션 만료 정책은 구현 시 결정)

> ⚠️ **보안 참고사항**: 정적 프론트엔드 코드에 비밀번호를 그대로 하드코딩하면, 브라우저 개발자도구(소스보기/네트워크 탭)로 누구나 값을 확인할 수 있어 실질적인 보안 효과가 거의 없습니다. **사내 관계자만 접근한다는 전제하의 최소한의 진입장벽(비공식 열람 방지용)**으로는 충분하지만, 아래 전제가 깨지면 반드시 보완이 필요합니다.
> - 대시보드 URL이 외부에 노출되거나, 검색엔진에 노출될 가능성이 있는 경우
> - 매출 등 민감 정보가 포함되어 있어 법적/사내 정책상 접근통제 수준이 요구되는 경우
>
> 위 경우, 비밀번호를 **서버(API) 단에서 검증**하도록 옮기거나(클라이언트에는 비밀번호 자체를 노출하지 않음), Google Workspace SSO 등 정식 인증 체계로 전환하는 것을 권장합니다. 초기 버전은 사용자 요청에 따라 하드코딩 방식으로 구현하되, 이 리스크는 리스크 테이블(8장)에도 기록해 둡니다.

## 5. 데이터 모델 (제안)

```json
{
  "updatedAt": "2026-07-31T09:00:00+09:00",
  "sourceSheetVersion": "v2.0",
  "totals": {
    "kpi_b": [...12개월...], "kpi_r": [...],
    "act_b": [...], "act_r": [...]
  },
  "products": {
    "비즈링": {
      "team": "biz",
      "kpi_b": [...], "kpi_r": [...],
      "act_b": [...], "act_r": [...],
      "excludeFromBillTotal": false
    },
    "용역사업": { "...": "...", "excludeFromBillTotal": true }
  },
  "snapshotHistory": [
    { "date": "2026-07-24", "totals": {...}, "products": {...} },
    { "date": "2026-07-31", "totals": {...}, "products": {...} }
  ]
}
```

- `excludeFromBillTotal` 같은 플래그를 데이터 모델에 명시적으로 포함시켜, "인식x" 같은 규칙이 코드에 하드코딩되지 않고 **데이터 기반으로 관리**되도록 함
- `snapshotHistory`를 자동 누적하여 주차별 비교를 위한 별도 캐싱(현재의 Claude 메모리 캐싱)을 제거

---

## 6. 비기능 요구사항

| 항목 | 요구사항 |
|---|---|
| **데이터 신선도** | 시트 변경 후 1시간 이내 웹 반영 (추후 Apps Script 트리거 적용 시 1분 이내로 단축 가능) |
| **가용성** | 웹 대시보드 99% 이상 접근 가능 (정적 호스팅 기준 무리 없는 목표) |
| **보안** | Google Sheets API 인증 정보(서비스 계정 키 등)는 CI/CD 시크릿으로 관리, 코드/리포지토리에 노출 금지. 대시보드 접근 범위(사내 전용 여부) 결정 필요 |
| **정확성** | 시트 구조 변경(행 추가/라벨 변경) 시 파싱 실패를 조용히 넘기지 않고 명시적 에러/알림 발생 |
| **유지보수성** | 데이터 파싱 로직과 화면 렌더링 로직을 분리한 모듈 구조 |

---

## 7. 단계별 로드맵 (제안)

| Phase | 내용 | 산출물 |
|---|---|---|
| **Phase 1** | 데이터/화면 분리: 현재 HTML의 하드코딩된 JS 상수를 외부 JSON으로 분리, 프론트는 fetch로 로딩. **기존 Claude 메모리 스냅샷(7/16~7/31) 을 `snapshotHistory` 초기값으로 이관** | 리팩터링된 코드베이스, 로컬에서 동작 확인 |
| **Phase 2** | Sync Job 구현: Google Sheets API로 시트를 읽어 JSON 생성하는 스크립트(1시간 폴링) (Claude Code로 개발) | `sync.js`(or `.py`) + 스케줄러 |
| **Phase 3** | 로그인 페이지 구현 + 자동 배포 파이프라인: GitHub Actions + **GitHub Pages** 연결 | 비밀번호 게이트, 고정 URL(`*.github.io`), 커밋/데이터 갱신 시 자동 배포 |
| **Phase 4** | 실시간성 고도화: 폴링 → Apps Script 트리거 방식 전환, 스냅샷 히스토리 자동 적재 | 주차별 비교 자동화 |
| **Phase 5** | 안정화: 에러 알림, 데이터 검증, 신규 상품 자동 대응 로직 | 모니터링/알림 체계 |
| **Phase 6 (향후)** | **사내 호스팅 서버로 이관**: GitHub Pages의 "URL 공개" 구조적 한계 해소 목적. 사내망 접근 제한 또는 서버단 인증으로 전환 | 사내 서버 배포, 접근 제어 강화 |

---

## 8. 리스크 및 대응

| 리스크 | 영향 | 대응 방안 |
|---|---|---|
| Google Sheets API 인증/쿼터 제한 | 동기화 실패 | 서비스 계정 사용, 폴링 주기 최적화, 실패 시 재시도·알림 |
| 시트 행/열 구조가 예고 없이 바뀜 (과거 실제 발생: "취급고(인식x)" 라벨 변경) | 잘못된 데이터 반영 | 파싱 시 셀 라벨 검증 단계 추가, 불일치 시 갱신 중단 + 알림 |
| 신규 상품 추가 시 팀 분류·KPI 존재 여부 등 예외 케이스 | 대시보드 누락/오류 | 상품 메타데이터(팀, KPI 존재 여부)를 시트 또는 설정 파일에서 명시적으로 관리 |
| 웹 공개 범위 (사내 전용 vs 외부 공개) 미정 | 보안/정보 노출 리스크 | **1차(GitHub Pages)는 URL 공개가 구조적 한계임을 인지하고 사용. Phase 6에서 사내 호스팅 서버로 이관해 근본 해결 예정** |
| 클라이언트에 하드코딩된 로그인 비밀번호 | GitHub Pages로 배포 시 URL이 사실상 공개이므로, 소스보기만으로 비밀번호가 그대로 노출됨 → 사실상 "누구나 열람 가능"과 동일 | 사내 서버 이관(Phase 6) 전까지의 **임시 조치**로 인지하고 운영. 이관 시 서버단 인증으로 전환 |

---

## 9. 성공 지표

- 대시보드 갱신을 위한 **수동 개입(Claude 대화) 횟수 0회/주** (완전 자동화)
- 시트 변경 → 웹 반영까지 **평균 지연 시간 1시간 이내**
- 갱신 누락(예: KPI 데이터 탭 미반영) 사고 **0건**
- 팀 내 대시보드 URL 접근 빈도로 활용도 측정

---

## 10. 확정된 의사결정 (2026-07-31)

| 항목 | 결정 |
|---|---|
| **접근 권한** | 별도 로그인 페이지를 두고, 고정 비밀번호 입력 시 대시보드 열람 가능한 구조로 구현 (상세는 4.4 참고) |
| **개발/운영 담당** | Robin이 직접 개발·운영 (Claude Code 활용) |
| **과거 스냅샷 이관** | 기존 Claude 메모리에 있던 스냅샷(7/16, 7/23, 7/24, 7/30, 7/31)을 `snapshotHistory` 초기 데이터로 이관 |
| **호스팅 환경** | 1차: GitHub 활용 (GitHub Pages 정적 배포 + GitHub Actions CI/CD). **추후 사내 호스팅 서버로 이관 예정**(보안 리스크 해소 목적) |
| **Google Sheets API 접근 권한** | 확보됨 (Robin 본인 계정/서비스 계정 권한 보유) |

## 11. Open Questions

없음 — 착수 전 확인 필요 항목 모두 확정됨
