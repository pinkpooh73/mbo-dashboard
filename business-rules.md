# 계산 규칙 & 운영 규칙 (Business Rules)

이 문서는 "미디어사업실 매출관리 대시보드"의 숫자가 **왜 이렇게 계산되는지**를
설명합니다. 처음 이 프로젝트를 맡는 사람이 코드를 뜯어보지 않고도 이 문서만
읽으면 계산 로직의 의도를 이해할 수 있는 것이 목표입니다. 코드가 바뀌면 이
문서도 같이 고쳐주세요 — 코드와 문서가 어긋나면 이 문서의 존재 의미가 없어집니다.

## 1. 단위

- 시트 원본 값은 **원(₩)** 단위입니다.
- `data.json`의 `products.*.kpi_b`/`kpi_r`/`act_b`/`act_r`와 각 `snapshotHistory`
  항목은 **만원** 단위입니다 (원 ÷ 10,000, 반올림).
- 화면에 "연간 취급고 KPI: 6,944,000,000"처럼 큰 숫자로 표시될 때는 만원 값을
  다시 ×10,000 해서 원 단위로 보여주는 것입니다. `index.html`의 `.toLocaleString()`
  앞에 `*10000`이 붙어 있는 곳이 전부 이 변환입니다.
- `quarterlyOverall.kpiWon`/`actWon`은 예외적으로 **원** 단위 그대로입니다 (시트의
  매출 KPI/실적 행을 원 단위 그대로 옮긴 것 — 분기별 전체 달성현황 차트에서 씀).

## 2. 취급고 총계 제외 규칙 (용역·다윈)

**규칙**: 용역, 다윈 두 상품의 **취급고 실적(매출 아님)** 은 전체/팀 합계·차트에서
항상 제외됩니다. 개별 상품 표(예: 미디어사업팀 탭의 "상품별 실적 상세")나 KPI
데이터 원본 탭에는 실제 값 그대로 표시됩니다 — **숨기는 게 아니라 합계 계산에서만
뺀다**는 점이 핵심입니다.

**정확히 무엇이 빠지는가**: 제외되는 건 **`act_b`(취급고 실적)뿐**입니다.
**`kpi_b`(취급고 KPI 목표)는 제외 상품까지 포함해 전부 합산**됩니다
(`index.html`의 `computeTotal()`/`computeTeam()`, 그리고 `sync/parseSheet.js`의
시트 총계 교차검증 `computedKpiB`가 모두 이 규칙입니다 — 시트의 총계 행 자체가
제외 상품을 포함해서 계산되기 때문에 여기서 빼면 오히려 교차검증이 깨집니다).
현재 용역·다윈은 취급고 KPI가 0이라 실질적인 차이가 없습니다. 다만 **나중에
이 상품들에 취급고 KPI가 잡히면 달성률(실적÷목표)의 분모만 커져서 달성률이
낮게 보일 수 있으니** 그때는 이 규칙을 다시 검토하세요.

**왜**: 이 두 상품은 매출을 인식하는 방식이 달라서(정확한 배경은 시트 관리자에게
문의), 취급고와 매출이 사실상 같은 숫자를 의미합니다. 전체 취급고 합계에 넣으면
매출과 이중 계산되는 셈이라 실제보다 취급고가 부풀려 보입니다.

**어떻게 판단하는가**: 상품명을 하드코딩해서 걸러내지 않습니다. 시트의 실적
상세 블록에서 그 상품의 "취급고" 행 라벨이 `취급고(인식x)`로 나오는지를 보고
자동으로 판단합니다 (`sync/parseSheet.js`의 `excludeFromBillTotal` 필드).
**"(인식x)"라는 문자열 자체가 시트가 우리에게 주는 신호**입니다 — 이 라벨이
용역·다윈이 아닌 다른 상품에 붙으면 그 상품도 자동으로 제외 대상이 되고,
반대로 이 라벨이 사라지면 자동으로 합계에 포함됩니다. 코드에 `if (name ===
'용역' || name === '다윈')` 같은 분기를 추가하지 마세요 — 그 순간 이 자동
감지가 무력화됩니다.

**매출(`kpi_r`/`act_r`)은 절대 제외되지 않습니다.** 제외는 취급고에만 적용됩니다.

**계산 위치**:
- 화면(`index.html`): `computeTotal()`/`computeTeam()`이 `excludeFromBillTotal`을
  보고 취급고 합계에서 뺍니다.
- 이상치 감지(`sync/detectAnomalies.js`)나 스냅샷(`sync/mergeSnapshot.js`)은
  원본 값을 그대로 보존합니다 — 제외 여부 판단은 항상 화면단 집계 시점에만
  적용됩니다.

## 3. 상품 메타데이터 (팀 소속, 이름 매핑)

시트에 쓰는 상품명과 대시보드가 쓰는 정식 이름이 다른 경우가 있습니다 (예:
시트 "스텔라이즈" ↔ 대시보드 "Stellaize", 시트 "용역사업(sk광고+커머스렙)" ↔
대시보드 "용역"). 이 매핑과 팀 소속(미디어사업팀=`biz` / 미디어마케팅팀=`mkt`)은
**[sync/productConfig.json](sync/productConfig.json)** 한 곳에서 관리합니다.

### 신규 상품 추가 절차

1. 시트에 새 상품 행이 추가되면, 다음 동기화 때 `sync/parseSheet.js`가 이
   상품을 인식은 하되 `productConfig.json`에 없으므로 `team: null`로
   반영하고 경고를 남깁니다(`dataQualityWarnings` + GitHub Actions
   `::warning::`). **대시보드에서 사라지지 않습니다** — 전체 합계에는 정상
   반영되고, 다만 미디어사업팀/미디어마케팅팀 탭에는 team이 없어서 안 뜹니다.
2. `productConfig.json`의 `products` 배열에 한 줄 추가:
   ```json
   { "sheetLabel": "시트에 적힌 이름 그대로", "key": "대시보드에서 쓸 이름", "team": "biz 또는 mkt" }
   ```
3. 코드 수정은 필요 없습니다. 다음 동기화부터 팀 탭에도 정상적으로 나타납니다.

취급고 KPI 존재 여부(`kpi_b`가 0인지 아닌지)는 설정 파일에 따로 적지 않습니다
— 시트의 KPI 요약 블록에 그 상품의 "(상품명)(취급고)" 행이 있는지 없는지로
자동 판단되기 때문입니다 (없으면 자동으로 `kpi_b`가 전부 0으로 채워짐).

## 4. "1~N월" 기준월

KPI 카드/섹션 제목에 나오는 "1~N월"은 **오늘 날짜의 달력 월**을 그대로
씁니다 (`index.html`: `N = 현재 달`, Asia/Seoul 기준이 아니라 브라우저의
로컬 시간대 — 사내에서만 보므로 문제 없음). 매달 1일이 되면 자동으로 N이
올라갑니다. 사람이 수동으로 갱신할 값이 아닙니다 — `data.json`의
`monthsElapsed` 필드는 더 이상 이 용도로 쓰이지 않는 legacy 필드입니다.

`sync/detectAnomalies.js`도 별도로 "현재 월"을 계산하지만, **시트 데이터
자체에서 유추하지 않고 달력에서 직접 계산**합니다(Asia/Seoul 기준,
`seoulMonthIndex()`). 이유: 용역 같은 일부 상품은 12월까지의 값이 미리
채워져 있는 경우가 있어서, "데이터에 값이 있는 마지막 달"을 현재 월로
오인하면 아직 오지도 않은 미래 달과 비교하게 되는 버그가 생깁니다(실제로
개발 중 발견한 버그 — `sync/test/detectAnomalies.test.js`의 "data
pre-filled into future months" 테스트가 이 케이스를 고정해둔 것입니다).

## 5. 주차별 스냅샷 (snapshotHistory)

- `sync/mergeSnapshot.js`가 동기화할 때마다 그 시점의 `products` 스냅샷을
  `data.json.snapshotHistory`에 쌓습니다.
- **하루(Asia/Seoul 기준 날짜)에 한 항목만** 유지됩니다. 같은 날 여러 번
  동기화되면(Phase 4의 실시간 웹훅 때문에 하루에도 여러 번 일어날 수 있음)
  그날 항목을 덮어씁니다 — 무한정 쌓이지 않고, "어제 대비 오늘"이라는
  비교가 의미 있게 유지됩니다.
- **보존 기간은 90일**입니다(`sync/mergeSnapshot.js`의 `RETENTION_DAYS`).
  하루 한 항목이라도 1년이면 1MB가 넘고, 이 파일은 대시보드를 여는 모든
  사람이 매번 내려받습니다. 화면이 실제로 쓰는 건 최신 2개뿐이라 90일치는
  넉넉한 여유분입니다. 단, 오래된 항목만 있는 상태(예: 동기화가 몇 달
  멈췄다가 재개)에서도 **최소 2개(`MIN_KEPT_ENTRIES`)는 남깁니다** — 비교
  대상이 하나도 없으면 "주차별 비교"가 빈 화면이 되기 때문입니다.
- "주차별 핵심 지표 비교"/"상품별 주요 변동" 탭은 `snapshotHistory`의 **가장
  최근 2개 항목**을 비교해서 화면에서 직접 계산합니다
  (`index.html`의 `computeWeeklyComparison()`/`computeWeeklyProductChanges()`).
  더 이상 사람이 수동으로 갱신하는 값이 없습니다.
- "상품별 주요 변동"에 어떤 상품이 나오는지는 **고정 개수(top-N)가 아니라,
  이번 달 취급고가 조금이라도 바뀐 상품 전부**를 변동폭 큰 순으로 보여주는
  것입니다. (과거 수작업 큐레이션 데이터와 대조해서 이 규칙이 실제로 같은
  결과를 낸다는 걸 확인한 뒤 채택했습니다.)

## 6. 이상치 감지 규칙 (`sync/detectAnomalies.js`)

매 동기화마다 아래 두 가지를 확인하고, 걸리면 Slack으로 알림을 보냅니다
(`SLACK_WEBHOOK_URL` 시크릿이 설정된 경우; 없으면 GitHub Actions 로그에
`::warning::`으로만 남습니다). **동기화 자체는 이상치가 있어도 실패하지
않습니다** — data.json은 정상적으로 갱신되고, 알림은 "확인해 보라"는
용도입니다.

### 6.1 취급고 == 매출 완전 동일

어떤 상품의 이번 달 취급고와 매출이 소수점까지 완전히 같은 값이면 의심스러운
신호입니다(시트에서 컬럼을 잘못 복사했을 때 실제로 발생했던 패턴). **다만
Stellaize처럼 원래부터 항상 취급고=매출인 상품도 있어서**, 무조건 걸리면
매달 오탐이 뜹니다. 그래서 "그 상품 자신의 과거 달"과 비교해서, 평소
(과거 실적이 있는 달의 50% 이상) 취급고=매출이 아니었는데 이번 달만 그런
경우에만 알림을 보냅니다. 이 로직은 실제 데이터(Stellaize)로 오탐이 없는지
테스트로 고정해뒀습니다.

### 6.2 전월 대비 급증/급감

어떤 상품의 취급고 또는 매출이 전월 대비 **5배 이상 증가**하거나 **1/5
이하로 감소**하면 알림 대상입니다. 예외:
- 전월이 0인 경우(신규 활성화)는 급증으로 보지 않습니다 — 정상적인 신규
  진입입니다.
- 이번 달이 정확히 0으로 떨어지는 경우는 급감으로 보지 않습니다 — 이
  시트에서는 소액 상품의 실적이 월 마감 전까지 0으로 남아있다가 나중에
  채워지는 패턴이 흔해서(실제 7월 데이터에서도 여러 상품이 이 패턴), 오타가
  아니라 "아직 입력 안 됨"일 가능성이 훨씬 높습니다. 진짜 오타라면 보통
  잘못된 값이 남지, 정확히 0이 되지는 않습니다.

임계값(5배/0.2배)은 `sync/detectAnomalies.js` 상단의 `SPIKE_UP_RATIO`/
`SPIKE_DOWN_RATIO` 상수로 관리합니다. 너무 시끄러우면 값을 조정하세요 —
실제 운영해보면서 튜닝이 필요할 수 있습니다.

### 알림 채널

Slack과 이메일 두 채널을 모두 지원하며(`sync/notify.js`가 둘 다 시도),
각각 독립적으로 설정 여부를 확인합니다 — 한쪽 설정이 없거나 실패해도 다른
쪽/동기화 자체에는 영향 없습니다.

- **Slack**: `SLACK_WEBHOOK_URL` (GitHub Actions 시크릿) — 인커밍 웹훅 URL.
- **이메일**: `SMTP_HOST`/`SMTP_PORT`/`SMTP_USER`/`SMTP_PASS`(시크릿),
  `SMTP_FROM`(시크릿, 생략 시 `SMTP_USER`로 발신), `ALERT_EMAIL_TO`(repo
  variable — 이메일 주소 자체는 민감정보가 아니므로 secret이 아닌 variable로
  관리). `sync/notifyEmail.js`가 nodemailer로 발송합니다.
- 연동만 따로 확인하고 싶으면 `cd sync && node scripts/testNotify.js` —
  실제 시트/동기화 없이 두 채널 모두에 테스트 메시지를 보내봅니다.

## 7. 동기화 범위 — sync가 갱신하는 필드 / 갱신하지 않는 필드

동기화 작업(`sync/sync.js`)은 **"미디어사업실_전체" 시트 탭 하나만** 읽습니다.
그래서 `data.json` 안의 모든 값이 매번 최신화되는 게 아닙니다. 헤더의 "마지막
업데이트" 시각은 **아래 '갱신함' 목록이 갱신된 시각**이라는 점을 기억하세요.

### 매 동기화마다 갱신되는 것

| 대상 | 출처 |
| --- | --- |
| `products` (상품별 `kpi_b`/`kpi_r`/`act_b`/`act_r`/`bill`/`rev`/`team`/`excludeFromBillTotal`) | 미디어사업실_전체 탭 |
| `quarterlyOverall` (`kpiWon`/`actWon`) | 같은 탭의 매출 총계 행 |
| `snapshotHistory` | 위 `products`의 그날 스냅샷 (§5) |
| `dataQualityWarnings` / `dataQualityAnomalies` | 파싱 경고 / 이상치 감지 결과 (§6) |
| `updatedAt` | 실제로 뭔가 바뀐 동기화의 시각 (아래 참고) |
| `raw-table.html` (별도 파일) | 같은 탭 전체를 표로 다시 렌더링 |

### 동기화가 **건드리지 않는** 것 (Phase 1 시점 값 그대로)

| 필드 | 왜 |
| --- | --- |
| `agencyRevenue` (대행사별 매출) | 다른 시트 탭에 있음 — 아직 파싱 대상 아님 |
| `campaignDetails` (월별 캠페인) | 다른 시트 탭에 있음 — 아직 파싱 대상 아님 |
| `stellaH1BillOverride` | 시트의 특정 셀에서 사람이 옮겨 적은 고정값 |
| `monthsElapsed` | Phase 4 이후 화면이 달력에서 직접 계산 — 쓰이지 않는 legacy |
| `legacy` (`bRate`/`rRate`) | 최초 export 재현용으로만 남은 값 |
| `sourceSheetVersion` | 시트 버전 표기 — 사람이 바꿀 값 |

**즉 "대행사별 매출"과 "월별 캠페인" 탭의 숫자는 자동으로 최신화되지 않습니다.**
바꾸려면 `data.json`을 직접 수정해야 하고, 이 두 탭까지 자동화하려면 해당 시트
탭용 파서를 새로 만들어야 합니다(현재 범위 밖).

### "변경 없으면 아무것도 쓰지 않는다"

동기화는 위 '갱신함' 목록 중 **실제로 값이 달라진 게 있을 때만** 파일을
씁니다(`sync/detectChanges.js`). 시트가 그대로면 `data.json`/`raw-table.html`을
아예 건드리지 않고(따라서 커밋도, Pages 재배포도 없음) 로그에 "변경 없음"만
남깁니다. 그래서 **`updatedAt`은 "마지막으로 확인한 시각"이 아니라 "마지막으로
숫자가 바뀐 시각"** 입니다 — 이 값이 어제로 표시된다면 동기화가 멈춘 게 아니라
어제 이후로 시트에 바뀐 게 없다는 뜻입니다. (동기화가 정말 도는지는 GitHub
Actions의 실행 기록으로 확인하세요.)

무엇을 "실질 변경"으로 보는지: 상품 값 변경, 신규 상품 등장/사라짐,
`quarterlyOverall` 변경, 경고·이상치 목록 변경, `raw-table.html` 내용 변경,
스냅샷 이력 변경(새 날짜 항목 생성 또는 90일 초과분 정리). `updatedAt` 자체는
변경 판단에서 제외합니다 — 그게 바뀌었다고 다시 쓰기 시작하면 매번 무한히
갱신되는 원래 문제로 되돌아갑니다.

## 8. 분기별 상품 차트/표 포함 기준

KPI 달성현황 탭의 "취급고/매출 — 상품별 분기 달성률" 차트와 그 아래 표는
과거에는 상품명을 코드에 직접 나열해뒀었는데(`index.html`의 `rmap` 객체),
그러다 보니 새 상품(`리사이즈애드`)이 시트에 추가된 뒤에도 이 두 화면에는
반영되지 않는 회귀가 실제로 발생했습니다.

지금은 `compute.js`의 `quarterSegmentProducts(products, seg)`가 **데이터
기반으로** 포함 여부를 판단합니다: 그 세그먼트(취급고=`b`/매출=`r`)의 달성률
배열(`bill`/`rev`)에 0이 아닌 값이 하나라도 있거나, 해당 세그먼트 KPI
(`kpi_b`/`kpi_r`)가 잡혀 있으면 포함됩니다. 즉 **KPI도 없고 달성률도 전부
0%인 상품(예: 현재의 `기타`, `리사이즈애드`)은 이 두 화면에서만 자동으로
빠집니다** — "분기 달성률"이라는 지표 자체가 의미가 없어서(분모가 없음)
빼는 것이며, 상품명을 걸러내는 하드코딩이 아닙니다. 시트에서 그 상품에
KPI가 잡히는 순간, 코드 수정 없이 자동으로 나타납니다.

다른 화면(팀별 탭의 "상품별 실적 상세" 표, 상품별 차트, 전체/팀 합계 카드,
KPI 데이터 원본 탭)에는 이 필터가 적용되지 않습니다 — 그 상품들은 이미 항상
정상 표시됩니다.

## 9. 이 문서에 없는 것

- Sync Job 인증정보 발급/등록 절차, Apps Script 웹훅 설정 절차 → 대화
  히스토리(Phase 2/4 완료 보고) 또는 [CLAUDE.md](CLAUDE.md) 참고.
- 각 Phase가 언제/왜 이렇게 결정됐는지의 전체 맥락 → PRD 문서
  ([PRD_미디어사업실_매출관리_대시보드_5Phase_ClaudeCode.md](PRD_미디어사업실_매출관리_대시보드_5Phase_ClaudeCode.md)).
